services:
  web:
    image: ghcr.io/greenlogles/proxui.app:latest
    container_name: proxui-web
    restart: unless-stopped
    ports:
      - "8088:80"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
